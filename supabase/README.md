# Supabase

- Add each schema change as a new timestamped file in `migrations/`.
- Keep secrets out of `config.toml`; reference environment variables when required.
- Link the hosted project only when you are ready to synchronize migrations.

import { Pressable, ScrollView, StyleSheet, Text } from "react-native";
export const DEFAULT_QUICK_PROMPTS=["Suggest a day itinerary","Food recommendations","Budget travel tips","Best time to visit"];
export function AiQuickPrompts({prompts=DEFAULT_QUICK_PROMPTS,disabled,onPrompt}:{prompts?:string[];disabled?:boolean;onPrompt(prompt:string):void}){return <ScrollView horizontal showsHorizontalScrollIndicator={false} contentContainerStyle={styles.track}>{prompts.slice(0,4).map((prompt)=><Pressable key={prompt} disabled={disabled} onPress={()=>onPrompt(contextual(prompt))} style={({pressed}:{pressed:boolean})=>[styles.chip,pressed&&styles.pressed,disabled&&styles.disabled]}><Text style={styles.text}>{prompt}</Text></Pressable>)}</ScrollView>}
function contextual(label:string){const lower=label.toLowerCase();if(lower.includes("food"))return "Recommend food and dining experiences relevant to the destination we are currently discussing.";if(lower.includes("budget"))return "Help me make the trip we are discussing more budget-efficient without ruining the experience.";if(lower.includes("best time"))return "What is the best time to visit the destination we are currently discussing, and what tradeoffs should I know?";if(lower.includes("day"))return "Suggest a realistic one-day itinerary for the destination we are currently discussing, keeping travel time sensible.";return label;}
const styles=StyleSheet.create({track:{gap:7,paddingVertical:8},chip:{minHeight:33,alignItems:"center",justifyContent:"center",paddingHorizontal:11,borderRadius:16,backgroundColor:"#F8F5FF",borderWidth:1,borderColor:"#E7E0FA"},text:{color:"#6855BB",fontSize:8,fontWeight:"800"},pressed:{opacity:.68},disabled:{opacity:.5}});

# Architecture

Trava AI uses an npm-workspaces monorepo. Mobile route files remain thin, feature logic stays inside feature folders, private keys stay inside the API workspace, and shared code remains platform-independent.
